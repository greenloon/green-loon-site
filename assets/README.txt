Save your logo here as:  logo.png

The hero and nav reference assets/logo.png. Until that file exists, the page
shows a built-in inline-SVG loon mark as a fallback, so nothing looks broken.

Tips:
- A transparent-background PNG or SVG looks best on the dark green background.
- Aim for at least ~200px tall so it stays crisp on high-DPI screens.
- If your file has a different name or format, tell me and I'll update the
  src="assets/logo.png" reference in index.html (two places: nav uses an inline
  mark; the hero uses this image).
